sudo: required
services:
  - docker

before_install:
  # Build development images to test them
  - docker build -t okfiera/react-test -f ./client/Dockerfile.dev

script:
  # Test development images
  - docker run okfiera/react-test npm test -- --coverage

after_success:
  # Build production images
  - docker build -t okfiera/multi-client ./client
  - docker build -t okfiera/multi-nginx ./nginx
  - docker build -t okfiera/multi-server ./server
  - docker build -t okfiera/multi-workder ./workder
  # Log into the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push okfiera/multi-client
  - docker push okfiera/multi-nginx
  - docker push okfiera/multi-server
  - docker push okfiera/multi-workder
